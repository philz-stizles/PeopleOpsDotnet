@page
@model PeopleOps.UI.Pages.Admin.Employees.IndexModel
@{
    Layout = "~/Pages/Shared/_DashboardLayout.cshtml";
    ViewData["ActivePage"] = "Employees";

}
<div class="card card-solid">
    <div class="card-header">
        <div class="d-flex justify-content-end">
            <a class="btn btn-success" asp-page="/Admin/Employees/Upsert">
                Create New <i class="fa fa-plus"></i>
            </a>
        </div>
    </div>
    <div class="card-body pb-0">
        <div class="row">
            <!--<div class="d-flex justify-content-end my-3">
                <!--<div class="d-flex align-items-center w-40">-->
            <!--@*<span class="d-block">Search:</span>&nbsp;*@-->
            <!--<input type="search" class="form-control form-control-sm" placeholder="Search" />
            </div>-->
            <!--
            </div>-->
            @foreach (var item in Model.Employees)
            {
                <partial name="_EmployeePartial" model="@item" />
            }
        </div>

        @*<div class="d-flex justify-content-between my-4">
                <div>Showing 1 to 3 of @Model.Employees.Count() entries</div>
                <div>
                    <span>Previous</span>&nbsp;&nbsp;
                    @for (int i = 1; i < 5; i++)
                    {
                        if (i >= 4)
                        {
                            <a style="color: #d8ddef" class="btn btn-sm btn-outline">....</a>
                            break;
                        }
                        <a class="btn btn-sm btn-outline-secondary">@i</a>
                    }
                    &nbsp;&nbsp;<span>Next</span>
                </div>
            </div>*@
    </div>
    <div class="card-footer">
        <nav aria-label="Employees Page Navigation">
            <ul class="pagination justify-content-center m-0">
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item"><a class="page-link" href="#">4</a></li>
                <li class="page-item"><a class="page-link" href="#">5</a></li>
                <li class="page-item"><a class="page-link" href="#">6</a></li>
                <li class="page-item"><a class="page-link" href="#">7</a></li>
                <li class="page-item"><a class="page-link" href="#">8</a></li>
            </ul>
        </nav>
    </div>
</div>